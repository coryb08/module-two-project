<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">

    <br>

    <%= form.text_field :email, {placeholder: 'edit email:', class: 'transparent-input focus', size:59}  %>
  </div>
<br>

  <div class="field">

    <%= form.text_field :password, {placeholder: 'edit password:', class: 'transparent-input focus', size:59}  %>
  </div>
<br>


  <div class="actions">
    <%= form.submit "update", {class: "button45"}%>
  </div>
<% end %>
